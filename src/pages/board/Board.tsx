import {SidePanel} from 'feature'
import {BoardContainer} from './boardStyled'

export const Board = (): JSX.Element => {
  return (
    <BoardContainer>
      <SidePanel/>
    </BoardContainer>
  )
}
